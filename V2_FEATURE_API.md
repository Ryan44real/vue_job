# BookSys V2.0 新功能接口文档

> 新增功能：图书预约与排队机制 / 动态信用与借阅额度系统
> 基准地址：`http://localhost:8080` | 认证方式：Header `Authorization: <token>`

---

## 一、图书预约与排队机制

### 1.1 业务规则

| 规则 | 说明 |
|------|------|
| 触发条件 | 图书状态为 `已借出` 或 `预约锁定` 时，前端展示"预约排队"按钮 |
| 队列算法 | FIFO（先进先出），按预约时间排序 |
| 自动锁定 | 图书归还时若有预约队列，自动转为 `预约锁定`，不进入公共可借池 |
| 通知方式 | 163 SMTP 邮件通知排队第一人 |
| 保留时长 | 通知后保留 **24 小时**，超时自动顺延下一位 |
| 信用门槛 | 信用分 ≥ 60 分才可预约 |

### 1.2 数据模型

```typescript
interface ReservationRecord {
  id: number;
  userId: number;
  username: string;          // 查询时填充
  bookNum: number;
  bookName: string;          // 查询时填充
  coverImg: string;          // 查询时填充
  reservationTime: string;   // "yyyy-MM-dd HH:mm:ss"
  notifyTime: string;        // 通知时间，未通知时为null
  status: number;            // 1=排队中, 2=已通知, 3=已失效, 4=已取消
  queuePosition: number;     // 队列位置（实时计算）
}
```

### 1.3 接口清单

#### 提交预约

```
POST /reservation/add
认证: 需要
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| bookNum | int | 是 | 图书编号 |

**前置条件**：图书状态为 `已借出` 或 `预约锁定`，且用户信用分 ≥ 60

**失败响应**：
| message | 原因 |
|---------|------|
| 该图书当前可借阅，无需预约，直接借阅即可 | 图书有库存 |
| 您的信用分(50)不足60分，无法使用预约功能 | 信用分不够 |
| 您已预约过该图书，请勿重复预约 | 重复预约 |

**调用示例**：
```js
fetch('/reservation/add', {
  method: 'POST',
  headers: { 'Authorization': token },
  body: 'bookNum=100001'
})
```

---

#### 我的预约队列

```
GET /reservation/myQueue
认证: 需要
```

无参数，返回当前登录用户的所有预约记录（含排队位置）。

**响应示例**：
```json
{
  "code": 0,
  "data": [
    {
      "id": 5,
      "userId": 2,
      "username": "reader01",
      "bookNum": 100001,
      "bookName": "百年孤独",
      "coverImg": "https://...",
      "reservationTime": "2026-05-04 10:30:00",
      "notifyTime": null,
      "status": 1,
      "queuePosition": 2
    }
  ]
}
```

> `queuePosition`：第 1 位表示你是下一个可借的读者

---

#### 图书预约队列（管理员）

```
GET /reservation/bookQueue
认证: 需要（管理员）
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| bookNum | int | 是 | 图书编号 |

---

#### 全部预约记录（管理员）

```
GET /reservation/all
认证: 需要（管理员）
```

无参数，返回系统中所有预约记录。

---

## 二、动态信用与借阅额度系统

### 2.1 业务规则

#### 信用等级与借阅额度

| 等级 | 积分范围 | 借阅额度 | 预约权限 |
|------|----------|----------|----------|
| 资深读者 | > 120 | 10 本 | 可以 |
| 普通读者 | 80 - 120 | 6 本 | 可以 |
| 受限读者 | 60 - 79 | 2 本 | **不可以** |
| 封禁名单 | < 60 | 0 本 | 不可以 |

- 新用户初始信用分：**100 分**
- 管理员可手动解封冻结账户

#### 积分变动规则

| 行为 | 变动 | 备注 |
|------|------|------|
| 按时归还 | **+2 分/本** | 幂等：同一条借阅记录只加一次 |
| 逾期归还 | **-5 分/天** | 阶梯式扣分 |
| 预约未取 | **-10 分/次** | 超24h未借阅触发，幂等 |
| 书评通过（预留） | **+1 分/条** | 需管理员审核 |

#### 积分变动日志

```typescript
interface CreditLog {
  id: number;
  userId: number;
  changeAmount: number;  // 正数加分，负数减分
  reason: string;        // "按时归还" / "逾期归还(3天)" / "预约未取" / "书评奖励"
  createTime: string;    // "yyyy-MM-dd HH:mm:ss"
  relatedId: number;     // 关联的借阅记录ID / 预约记录ID
}
```

### 2.2 现有接口行为变更

#### 借阅图书 `GET /borrow/borrowBook`

**变更点**：硬编码"最多6本"改为动态信用额度。

**新增校验**：
1. 计算当前用户信用分对应的借阅额度
2. 已借数量 ≥ 额度时返回：`借阅已达上限(2本)，当前信用等级：受限读者(65分)`
3. 信用分 < 60 时返回：`您的信用分(45)过低，账户已冻结，请联系管理员`

#### 归还图书 `POST /borrow/returnBook`

**新增逻辑**（按顺序执行）：
1. 更新借阅记录为"已归还"，图书状态为"可借阅"
2. 信用分 +2（按时归还）
3. 检查该书的预约队列
4. 若有排队者，图书状态转为"预约锁定"，通知排队第一人
5. 若无排队者，图书进入公共可借池

### 2.3 图书状态枚举

| 状态值 | 含义 | 流通性 |
|--------|------|--------|
| `可借阅` | 在库，可被任何人借阅 | 公共可借 |
| `已借出` | 已被借走 | 不可借，可预约 |
| `预约锁定` | 归还后为预约者保留 24h | **仅预约人可借** |
| `草稿` | 未上架 | 不可操作 |
| `已发布` | 已上架但未设定借阅 | 视同可借阅 |

### 2.4 `borrowBook` 完整校验链（优先级从高到低）

```
1. userState == 2          → "该用户当前不允许借阅"
2. creditScore < 60        → "账户已冻结，请联系管理员"
3. 有逾期未还              → "存在逾期书籍，请先归还"
4. 已达信用额度上限        → "借阅已达上限(N本)，当前信用等级：..."
5. 图书为"预约锁定"且非本人 → "该图书已被预约锁定，仅预约人可借阅"
6. 图书为"已借出"          → "该书籍已经借出"
7. ✓ 借阅成功
```

---

## 三、定时任务变更

### 3.1 新增任务

| 任务 | Cron | 说明 |
|------|------|------|
| `processExpiredReservations` | `0 0 * * * ?` | 每小时检查过期预约（超24h），标记失效并**扣10分**，顺延至下一位 |
| `doTask`（原有） | `30 10 1 * * ?` | 每日凌晨 1:10 检查逾期（不变） |

### 3.2 预约过期处理流程

```
预约过期 → 标记 status=3（失效）
         → 扣除信用分 -10（幂等：同一预约ID只扣一次）
         → 通知下一位排队者
         → 若还有排队者，图书保持"预约锁定"
         → 若没有排队者，图书恢复"可借阅"
```

---

## 四、前端对接要点

### 4.1 图书详情页状态判断

```js
// 根据图书状态展示不同按钮
if (article.state === '可借阅') {
  showButton('借阅');
} else if (article.state === '已借出' || article.state === '预约锁定') {
  if (user.creditScore >= 60) {
    showButton('预约排队');
  } else {
    showText('信用分不足，无法预约');
  }
}
```

### 4.2 借阅时提示信用额度

```js
// 调用 borrowBook 前先查询用户信用额度
// borrowBook 失败时根据 message 给出不同提示：
if (msg.includes('借阅已达上限')) {
  // 显示当前额度上限和信用等级
  showDialog('您的信用等级为' + tier + '，最多可借' + limit + '本');
} else if (msg.includes('账户已冻结')) {
  showDialog('账户已冻结，请联系管理员');
}
```

### 4.3 预约排队进度展示

```js
// 调用 GET /reservation/myQueue 获取排队信息
// queuePosition === 1 时特殊提示："你是下一个！请留意邮件通知"
```

### 4.4 预约锁定借阅

```js
// 当图书为"预约锁定"时，仅预约人本人可借
// 借阅成功后自动清除对应预约记录（status=4）
```

---

## 五、数据库变更摘要

执行 `src/main/resources/db/init.sql` 即可完成建表。

### 新增表

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `reservation_record` | 预约排队记录 | userId, bookNum, status(1/2/3/4), reservation_time, notify_time |
| `credit_log` | 信用积分变动日志 | userId, change_amount, reason, related_id |

### 用户表扩展

`user` 表新增 `credit_score INT DEFAULT 100` 列。
