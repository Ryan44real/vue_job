

**任务：重构图书管理系统全部页面**

**核心约束（必须严格遵守）**
- 保留原有页面的所有功能，不得遗漏或改变现有功能逻辑。
- 在功能完整的前提下，可优化交互体验与信息架构，但不得删减任何已有操作入口。

**项目定位**
- 目标用户：在校大学生
- 核心场景：图书检索、借阅管理、个人书单、到期提醒、续借操作、阅读记录等典型校园图书馆使用场景
- 需兼顾高频查询（如馆藏检索、借阅状态）与低频但关键的操作（如续借、预约到馆取书）

**品牌风格与视觉倾向**
- 整体风格：现代化简洁，符合高校审美，避免过度花哨
- 必须支持浅色/深色（黑白）两种模式，且所有页面在两种模式下均需保持清晰可读与视觉一致性
- 信息层级分明，善用留白与卡片式布局，提升浏览效率

**技术规范**
- 技术栈：Vue 3 + TypeScript + Vite
- 采用 Composition API 编写组件，充分利用 TypeScript 类型约束
- 页面结构需模块化、可维护，路由设计清晰

**页面覆盖范围（如与原系统有差异，以原系统为准）**
- 首页/工作台：快捷入口（借阅、续借、检索）、到期提醒概览
- 馆藏检索与结果列表：支持多条件筛选（书名、作者、ISBN、分类）
- 图书详情页：图书信息、馆藏状态、借阅按钮/预约入口
- 个人中心：当前借阅列表、借阅历史、逾期记录
- 续借/预约操作页：确认续借条件、提示到期日变更
- 消息/通知中心：到期提醒、预约到馆、系统公告
- 设置页：主题切换（浅色/深色）、个人信息管理



## 一、功能清单对照表

重构前后需逐项核对，确保功能零遗漏。

| 模块 | 功能点 | 原系统状态 | 重构要求 | 验收标准 |
|------|--------|-----------|---------|---------|
| **首页/工作台** | 快捷入口（借阅、续借、检索） | 保留 | 保留并优化视觉呈现 | 三个入口均可正常跳转 |
| | 到期提醒概览（首页卡片） | 保留 | 保留，增强视觉警示 | 有即将到期/已逾期图书时正确展示 |
| | 今日图书馆开放时间/公告 | 如有则保留 | 保留 | 信息准确同步 |
| **馆藏检索** | 关键词检索（书名/作者/ISBN） | 保留 | 保留，增加搜索历史/热门搜索 | 三种字段均可检索并返回正确结果 |
| | 分类筛选（中图分类号/学科） | 保留 | 保留，可增加折叠式筛选面板 | 点击分类可正确过滤结果 |
| | 高级检索（出版年、出版社、语种等） | 如有则保留 | 保留 | 组合条件可正确过滤 |
| | 检索结果列表（分页/排序） | 保留 | 保留，支持列表/卡片视图切换 | 分页正常，排序选项（相关度、出版时间、借阅热度）有效 |
| | 结果条目显示：书名、作者、封面、馆藏地、可借数量 | 保留 | 保留，信息展示更清晰 | 各字段数据正确 |
| **图书详情页** | 图书基本信息（书名、作者、出版社、ISBN、简介、封面） | 保留 | 保留，布局优化 | 信息完整显示 |
| | 馆藏信息（总馆藏数、在馆数、具体馆藏地及索书号） | 保留 | 保留 | 各馆藏地数量正确 |
| | 借阅操作按钮（立即借阅） | 保留 | 保留，根据登录状态和可借数量动态置灰/高亮 | 有在馆可借时按钮可点击，无馆藏时显示"预约" |
| | 预约到馆取书 | 保留 | 保留 | 触发预约流程 |
| | 相关推荐/相似图书 | 如有则保留 | 保留 | 推荐内容合理加载 |
| | 收藏到个人书单 | 如有则保留 | 保留 | 点击后可加入/移出书单 |
| **个人中心** | 当前借阅列表（书名、借阅日期、应还日期、状态） | 保留 | 保留，增加剩余天数倒计时 | 借阅数据与后端一致 |
| | 借阅历史（含已还、逾期） | 保留 | 保留 | 支持分页查阅 |
| | 逾期记录与罚金/停借提示 | 如有则保留 | 保留 | 逾期状态正确标识 |
| | 个人书单/收藏 | 如有则保留 | 保留 | 可增删改查 |
| **续借操作** | 续借按钮入口（借阅列表内/详情页） | 保留 | 保留 | 符合续借条件时按钮可点击 |
| | 续借确认（提示新到期日） | 保留 | 保留，弹窗或页面均可 | 显示续借后新到期日 |
| | 续借失败提示（已达上限/有人预约/逾期） | 保留 | 保留，给出明确原因 | 不同失败原因显示对应文案 |
| **预约操作** | 预约到馆取书 | 保留 | 保留 | 成功后显示预计可取书时间 |
| | 预约列表/取消预约 | 保留 | 保留 | 可查看并取消预约 |
| | 预约到馆通知 | 保留 | 保留 | 收到通知后可查看 |
| **消息/通知中心** | 到期提醒（还书日临近） | 保留 | 保留，可配置提前提醒天数 | 在设定天数前推送 |
| | 逾期提醒 | 如有则保留 | 保留 | 逾期当日及后续定时提醒 |
| | 预约到馆通知 | 保留 | 保留 | 可到馆取书时推送 |
| | 系统公告 | 如有则保留 | 保留 | 展示图书馆公告 |
| **设置页** | 浅色/深色主题切换 | 新增 | 必须实现，默认跟随系统 | 切换后全站即时生效 |
| | 个人信息管理（昵称、头像、联系方式等） | 如有则保留 | 保留 | 可编辑保存 |
| | 修改密码 | 如有则保留 | 保留 | 走验证流程 |
| | 消息推送开关 | 如有则保留 | 保留 | 到期提醒/公告等可独立开关 |
| **全局** | 登录/登出 | 保留 | 保留，集成校园统一认证或独立账号 | 登录后方可使用核心功能 |
| | 响应式适配 | 如有则保留 | 必须保留，移动端优先适配 | 手机端可正常操作借阅流程 |
| | 权限控制（学生/教师/管理员） | 如有则保留 | 保留 | 不同角色显示不同菜单项 |

---

## 二、页面路由规划

基于 Vue Router 的路径设计，组件命名遵循 `PascalCase`，按模块分层。

```
/                          → 重定向到 /home
/home                      → HomeView.vue             首页/工作台
/search                    → SearchView.vue           馆藏检索页
/search/advanced           → AdvancedSearchView.vue   高级检索（如有）
/book/:id                  → BookDetailView.vue       图书详情页
/user                      → UserLayout.vue           个人中心布局（嵌套路由）
  /user/borrowing          → BorrowingListView.vue    当前借阅
  /user/history            → BorrowingHistoryView.vue 借阅历史
  /user/overdue            → OverdueView.vue          逾期记录
  /user/wishlist           → WishListView.vue         个人书单/收藏
  /user/reservations       → ReservationListView.vue  我的预约
/renew/:borrowId?          → RenewView.vue            续借操作页（可带借阅ID参数）
/reserve/:bookId?          → ReserveView.vue          预约操作页（可带图书ID参数）
/notifications             → NotificationView.vue     消息/通知中心
/settings                  → SettingsView.vue         设置页
  /settings/profile        → ProfileEditView.vue      个人信息编辑（可模态实现）
  /settings/password       → PasswordChangeView.vue   修改密码（可模态实现）
/auth/login                → LoginView.vue            登录页
/auth/register             → RegisterView.vue         注册页（如有）
/*                         → NotFoundView.vue         404页面
```

**路由层级说明：**
- `/user` 使用嵌套路由共用个人中心侧边栏/Layout
- `/renew` 和 `/reserve` 独立页面，也支持从借阅列表或详情页以路由参数直达
- `/search/advanced` 作为子路由，也可改为 `/search` 页面内的 Tab 或面板实现
- 所有 `如有则保留` 的路由，以原系统实际存在为准，不可删除原有路由

