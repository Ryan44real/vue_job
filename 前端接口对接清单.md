# 前后端接口对接清单

> 后端接口来源：`API_DOCUMENTATION.md`（基于 SpringDoc v3/api-docs）  
> 前端接口来源：`src/api/*.ts`   
> 核对日期：2026-05-04

---

## 状态标记说明

| 标记 | 含义 |
|------|------|
| ✅ 已完成 | 前端已实现、参数匹配 |
| ⚠️ 参数不匹配 | 前端已实现但参数名/类型与后端不一致 |
| ❌ 前端未完成 | 后端有、前端未实现 |
| 🔷 后端未完成 | 前端有调用、文档中不存在 |

---

## 1. 用户管理 /user（16 个端点）

| # | 后端接口 | 方法 | 认证 | 前端函数 | 文件 | 状态 |
|---|---------|------|------|---------|------|------|
| 1 | `/user/login` | POST | 否 | `userLoginService` | `api/user.ts:17` | ✅ |
| 2 | `/user/register` | POST | 否 | `useRegisterService` | `api/user.ts:6` | ✅ |
| 3 | `/user/sendmail` | GET | 否 | `sendMailCon` | `api/user.ts:44` | ✅ |
| 4 | `/user/editPswByEmail` | POST | 否 | `editPswByEmailService` | `api/user.ts:90` | ✅ |
| 5 | `/user/userInfo` | GET | 是 | `userInfoGetService` | `api/user.ts:27` | ✅ |
| 6 | `/user/update` | PUT | 是 | `userInfoUpdateService` | `api/user.ts:32` | ✅ |
| 7 | `/user/updateAvatar` | PATCH | 是 | `userAvatarUpdateService` | `api/user.ts:37` | ✅ |
| 8 | `/user/updatePwd` | PATCH | 是 | `resetPsw` | `api/user.ts:49` | ✅ |
| 9 | `/user/getUserList` | GET | 是(管理员) | `getUserListService` | `api/user.ts:58` | ✅ |
| 10 | `/user/editUser` | PATCH | 是(管理员) | `editUserService` | `api/user.ts:62` | ✅ |
| 11 | `/user/upgradeUserState` | GET | 是(管理员) | `upgradeUserStateService` | `api/user.ts:66` | ✅ |
| 12 | `/user/deleteUser` | GET | 是(管理员) | `deleteUserService` | `api/user.ts:82` | ✅ |
| 13 | `/user/userInfoForborrow` | GET | 是 | `getBorrowRecordInfoService` | `api/user.ts:54` | ✅ |
| 14 | `/user/getUserNumService` | GET | 是 | `getUserNumService` | `api/user.ts:86` | ✅ |
| 15 | `/user/deleteUserMsg` | GET | 是 | `deleteMsgService` | `api/user.ts:74` | ✅ |
| 16 | `/user/check` | GET | 是 | `checkLogin` | `api/user.ts:78` | ✅ |

**用户管理模块：16/16 全部完成** ✅

---

## 2. 图书管理 /article（12 个端点）

| # | 后端接口 | 方法 | 认证 | 前端函数 | 文件 | 状态 |
|---|---------|------|------|---------|------|------|
| 1 | `/article` | POST | 是 | `bookAddService` | `api/book.ts:8` | ✅ |
| 2 | `/article` | PUT | 是 | `bookUpdateService` | `api/book.ts:16` | ✅ |
| 3 | `/article` | DELETE | 是 | `bookDeleteService` | `api/book.ts:20` | ✅ |
| 4 | `/article/getBookList` | GET | 否 | `bookListService` | `api/book.ts:4` | ✅ |
| 5 | `/article/detail` | GET | 是 | `BookDetailService` | `api/book.ts:12` | ✅ |
| 6 | `/article/getBookContent` | GET | 是 | `AiContent` | `api/book.ts:24` | ✅ |
| 7 | `/article/getBookBarcode` | GET | 是 | `getBookBarcode` | `api/book.ts:28` | ✅ |
| 8 | `/article/getBookBarcodeByPy` | GET | 是 | `getBookBarcodeByPy` | `api/book.ts:36` | ✅ |
| 9 | `/article/buildBookNumService` | GET | 是 | `buildBookNumService` | `api/book.ts:32` | ✅ |
| 10 | `/article/buildQrCode` | GET | 是 | `buildQrCodeService` | `api/book.ts:40` | ✅ |
| 11 | `/article/getBookNumService` | GET | 是 | `getBookNumService` | `api/user.ts:98` | ✅ |
| 12 | `/article/getBookNumUseService` | GET | 是 | `getBookNumUseService` | `api/user.ts:106` | ✅ |

**图书管理模块：12/12 全部完成** ✅

---

## 3. 借阅管理 /borrow（11 个端点）

| # | 后端接口 | 方法 | 认证 | 前端函数 | 文件 | 状态 |
|---|---------|------|------|---------|------|------|
| 1 | `/borrow/borrowBook` | GET | 是 | `borrowBookService` | `api/borrow.ts:3` | ⚠️ |
| 2 | `/borrow/returnBook` | POST | 是 | `returnBook` | `api/borrow.ts:11` | ✅ |
| 3 | `/borrow/getRecord` | GET | 是 | `borrowRecordListService` | `api/borrow.ts:7` | ✅ |
| 4 | `/borrow/applyRenewal` | GET | 是 | `applyRenewalService` | `api/borrow.ts:15` | ⚠️ |
| 5 | `/borrow/getApplyRenewalList` | GET | 是 | `getApplyRenewalListService` | `api/borrow.ts:19` | ✅ |
| 6 | `/borrow/passApply` | GET | 是(管理员) | `passApplyService` | `api/borrow.ts:23` | ✅ |
| 7 | `/borrow/rejectApply` | GET | 是(管理员) | `rejectApplyService` | `api/borrow.ts:27` | ✅ |
| 8 | `/borrow/editRecord` | POST | 是(管理员) | `send` | `api/borrow.ts:31` | ✅ |
| 9 | `/borrow/urge` | GET | 是(管理员) | `urge` | `api/user.ts:70` | ✅ |
| 10 | `/borrow/getAllBorrowNum` | GET | 是 | `getAllBorrowNumServie` | `api/user.ts:102` | ✅ |
| 11 | `/borrow/getMyRecordNumService` | GET | 是 | `getMyRecordNumService` | `api/user.ts:109` | ✅ |

**借阅管理模块：9/11 完成，2 个参数问题** ⚠️

---

## 4. 分类管理 /category（5 个端点）

| # | 后端接口 | 方法 | 认证 | 前端函数 | 文件 | 状态 |
|---|---------|------|------|---------|------|------|
| 1 | `/category/add` | POST | 是 | `bookCategoryAddService` | `api/bookCategory.ts:11` | ✅ |
| 2 | `/category/query` | GET | 否 | `getBookCategory` | `api/bookCategory.ts:5` | ✅ |
| 3 | `/category/detail` | GET | 是 | — | — | ❌ |
| 4 | `/category` | PUT | 是 | `bookCategoryUpdateService` | `api/bookCategory.ts:16` | ✅ |
| 5 | `/category` | DELETE | 是 | `bookCategoryDeleteService` | `api/bookCategory.ts:21` | ✅ |

**分类管理模块：4/5 完成，1 个前端未实现** ❌

---

## 5. 文件上传 /load（1 个端点）

| # | 后端接口 | 方法 | 认证 | 前端使用位置 | 状态 |
|---|---------|------|------|-------------|------|
| 1 | `/load` | POST | 是 | `BookEditDrawer.vue`、`SettingsView.vue`（el-upload action="/api/load"） | ✅ |

**文件上传：1/1 完成** ✅

---

## 6. 参数不匹配详情 ⚠️

### 6.1 `/borrow/borrowBook` — 参数名不匹配

| 对比 | 参数 1 | 参数 2 | 额外参数 |
|------|--------|--------|----------|
| **后端期望** | `bookId` (int, 图书编号) | `day` (int) | — |
| **前端发送** | `bookNum` (string) | `day` (string) | `bookName` (string) |

**调用位置**：
- `src/components/book/BookBorrowDialog.vue:23` → `{ bookNum, day, bookName }`
- `src/views/BookDetailView.vue:54` → `{ bookNum, day, bookName }`

**问题**：后端参数名是 `bookId`，前端发送的参数名是 `bookNum`。虽然后端文档描述 `bookId` 为"图书编号（bookNum）"，但参数名不匹配会导致后端无法正确接收。

**建议修复**：将前端 `borrowBookService` 调用的 `bookNum` 改为 `bookId`。

---

### 6.2 `/borrow/applyRenewal` — 缺少必要参数

| 对比 | 参数 1 | 参数 2 | 参数 3 | 额外参数 |
|------|--------|--------|--------|----------|
| **后端期望** | `bookNum` (int) | `day` (int) | `borrowRecordId` (int) | — |
| **前端发送**（BorrowingListView）| `bookNum` | `day` | `borrowRecordId` | `bookName` |
| **前端发送**（RenewView）| ✗ 缺失 | `day` | `borrowRecordId` | — |

**调用位置**：
- `src/views/user/BorrowingListView.vue:53-57` → 发送 `{ bookName, bookNum, borrowRecordId, day }` ✅
- `src/views/RenewView.vue:18` → 发送 `{ borrowRecordId, day }` ❌ 缺少 `bookNum`

**问题**：`RenewView.vue` 中续借时没有传递 `bookNum` 参数，后端要求此参数为必填。

**建议修复**：在 `RenewView.vue` 中补充 `bookNum` 参数（可从路由 query 或借阅记录中获取）。

---

## 7. 前端未完成的接口 ❌

### 7.1 `/category/detail` — 分类详情

- **后端接口**：`GET /category/detail?id={int}`
- **状态**：后端已实现，前端从未调用
- **影响**：低。前端当前通过 `/category/query` 获取全部分类列表，详情接口仅在需要按 ID 查单个分类时使用。当前业务无需此接口。
- **建议**：若后续编辑分类前需要获取最新详情，可在 `CategoryManageView.vue` 编辑时调用。

---

## 8. 补充发现

### 8.1 认证不一致 — `/category/query`

- **后端文档**：`/category/query` 为免认证接口
- **前端实现**：`getBookCategory()` 手动添加 `Authorization` 请求头
- **影响**：无。发送已存在的 token 不影响后端响应；未登录时 token 为空字符串，也不会报错。

### 8.2 `userInfoStore.info` 直接赋值 vs `setInfo` 方法

- **问题**：多处组件直接赋值 `userInfoStore.info = ...` 而非调用 `userInfoStore.setInfo(...)`
- **影响**：功能正常，但绕过 store 的封装方法。不影响 API 对接。

### 8.3 `/borrow/returnBook` — 可选参数未使用

- **后端期望**：`userId` (int, 可选) + `bookNum` (string, 必填)
- **前端发送**：仅 `bookNum`
- **影响**：无。后端文档标注 `userId` 为可选，不传则后端自动根据 token 识别当前用户。

### 8.4 响应字段名不一致

- **后端文档**：统一响应使用 `message` 字段  
- **前端代码**：多处使用 `result.msg` 或 `result.message`
- **影响**：取决于后端实际返回的字段名。若后端既返回 `message` 也返回 `msg`，则无影响；否则部分 UI 提示可能显示 `undefined`。
- **建议**：统一使用 `result.message`（与后端文档一致）。

---

## 9. 汇总统计

| 模块 | 后端端点数 | 前端已完成 | 参数不匹配 | 前端未完成 |
|------|-----------|-----------|-----------|-----------|
| 用户管理 /user | 16 | 16 | 0 | 0 |
| 图书管理 /article | 12 | 12 | 0 | 0 |
| 借阅管理 /borrow | 11 | 9 | 2 | 0 |
| 分类管理 /category | 5 | 4 | 0 | 1 |
| 文件上传 /load | 1 | 1 | 0 | 0 |
| **合计** | **45** | **42** | **2** | **1** |

**完成率：42/45 = 93.3%**

---

## 10. 建议修改清单

### 高优先级（影响核心功能）

| # | 文件 | 修改内容 |
|---|------|---------|
| 1 | `src/components/book/BookBorrowDialog.vue:23` | `bookNum` → `bookId` |
| 2 | `src/views/BookDetailView.vue:54` | `bookNum` → `bookId` |
| 3 | `src/views/RenewView.vue:18` | 补充 `bookNum` 参数 |

### 中优先级（不影响功能但应规范）

| # | 文件 | 修改内容 |
|---|------|---------|
| 4 | 全局 | `result.msg` → `result.message` 统一响应字段名 |
| 5 | `src/api/bookCategory.ts:7` | 移除多余的 `Authorization` 手动设置 |

### 低优先级（按需）

| # | 文件 | 修改内容 |
|---|------|---------|
| 6 | `src/views/admin/CategoryManageView.vue` | 编辑时可先调 `/category/detail` 获取最新数据 |
